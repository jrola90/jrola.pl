'use strict';

describe('myApp.cv module', function() {

  beforeEach(module('myApp.cv'));

  describe('cv controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('CvCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});